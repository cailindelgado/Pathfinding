#include "libdarray.h"
#

darray *create_array(size_t initialCapacity) {

    darray *arr = NULL;
    // array = (darray *)
    arr = (darray *) safe_malloc(sizeof (int));
    assert (arr != NULL);
    arr -> array
    return (array);
}
